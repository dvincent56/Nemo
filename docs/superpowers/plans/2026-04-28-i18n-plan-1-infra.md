# i18n — Plan 1 : Infrastructure

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Installer `next-intl` v4, mettre en place le routing par locale (config + composition dans `proxy.ts`), créer les 4 fichiers de messages avec un namespace `common` minimal, créer une page shell `/[locale]` placeholder, ajouter la règle ESLint `react/jsx-no-literals` (scoped sur `[locale]/**`), et créer le script `pnpm i18n:check` + step CI. **Aucune route existante ne change** : `/marina`, `/races`, etc. continuent à fonctionner exactement comme avant.

**Architecture:** `next-intl` est intégré côté server via `createNextIntlPlugin` (wrap de `next.config.ts`) et côté request via `getRequestConfig`. Le middleware d'i18n est **composé** dans le `proxy.ts` existant (qui gère déjà l'auth) avec un matcher restreint à `/(fr|en|es|de)(/.*)?` — donc seules les nouvelles URLs préfixées passent par le pipeline i18n. Le script CI `check-translations` parse les usages `useTranslations()` / `t()` / `tEvent()` via le compilateur TypeScript (déjà en dépendance) pour détecter clés manquantes/orphelines/incohérentes entre locales.

**Tech Stack:** `next-intl` v4 (à installer), Next.js 16.2.3, React 19.2, TypeScript 5.7 strict, ESLint 9 (flat config), Vitest 4 (déjà en place), pnpm 9.12.

**Spec reference:** [docs/superpowers/specs/2026-04-28-i18n-design.md](../specs/2026-04-28-i18n-design.md)

---

## File structure

**New files:**

```
apps/web/src/i18n/routing.ts                      — locales tuple, defaultLocale, exported routing config
apps/web/src/i18n/routing.test.ts                 — vitest smoke test
apps/web/src/i18n/request.ts                      — server-side getRequestConfig (loads messages by locale)

apps/web/messages/fr.json                         — locale source (1-2 clés common)
apps/web/messages/en.json                         — stub identique
apps/web/messages/es.json                         — stub identique
apps/web/messages/de.json                         — stub identique

apps/web/src/app/[locale]/layout.tsx              — placeholder layout, fournit NextIntlClientProvider
apps/web/src/app/[locale]/page.tsx                — placeholder page ("hello {locale}")

apps/web/scripts/check-translations.ts            — script CI de complétude des traductions
apps/web/scripts/check-translations.test.ts       — vitest tests pour le parser et la validation
apps/web/scripts/__fixtures__/                    — fixtures TSX/JSON pour les tests
```

**Modified files:**

```
apps/web/package.json                             — ajoute next-intl, ajoute script i18n:check
apps/web/next.config.ts                           — wrap config avec createNextIntlPlugin
apps/web/proxy.ts                                 — compose next-intl middleware (matcher /(fr|en|es|de)(/.*)?)
apps/web/eslint.config.mjs                        — règle react/jsx-no-literals scopée à src/app/\\[locale\\]/**
apps/web/vitest.config.ts                         — ajoute scripts/**/*.test.ts à l'include
.github/workflows/ci.yml                          — ajoute step "pnpm --filter @nemo/web i18n:check"
CLAUDE.md                                         — ajoute section "i18n" avec règle "plus jamais de string en dur"
```

**Out of scope (Plans 2-7):**
- Déplacement des routes existantes vers `[locale]/` (Plan 2)
- Migration des composants `components/ui/` (Plan 3)
- Migration des pages publiques, joueur, /play (Plans 4-6)
- Refacto game-balance + neutralité backend (Plan 7)
- **Sélecteur de langue dans le footer** : la spec PR 1 le mentionne, mais il est déplacé en Plan 2 par déviation explicite. Rationale : tant que les routes ne sont pas sous `[locale]/`, cliquer un bouton de langue ne peut rien changer visiblement (le cookie `NEMO_LOCALE` n'est lu nulle part, et l'URL ne change pas). Ajouter des boutons no-op dans `SiteFooter` (rendu sur 25 pages existantes) violerait "app inchangée fonctionnellement" pour zéro bénéfice utilisateur. Le sélecteur sera ajouté en Plan 2 en même temps que le matcher middleware s'élargit, où il sera fonctionnel dès le merge.

---

## Test infrastructure approach

Les tests utilisent **Vitest** (convention existante du projet, voir [apps/web/vitest.config.ts](apps/web/vitest.config.ts)). Pour `check-translations.ts`, les tests créent des fixtures `.tsx` et `.json` dans `apps/web/scripts/__fixtures__/` et appellent les fonctions exportées du script avec ces chemins. Pas de mocks — le script lit réellement les fichiers, ce qui valide aussi le parsing AST.

Vitest config doit être étendue pour inclure `scripts/**/*.test.ts` (actuellement scope = src/lib, src/hooks, src/components, src/app uniquement).

Run tests with: `pnpm --filter @nemo/web test`

---

## Task 1 : Install next-intl

**Files:**
- Modify: `apps/web/package.json`

- [ ] **Step 1 : Ajouter `next-intl` aux dépendances**

```bash
cd apps/web && pnpm add next-intl@^4
```

- [ ] **Step 2 : Vérifier que l'install s'est bien passée**

```bash
pnpm --filter @nemo/web list next-intl
```
Expected: line `next-intl 4.x.x`

- [ ] **Step 3 : Smoke test compatibilité Next 16.2 / React 19.2**

```bash
node -e "import('next-intl').then(m => console.log(Object.keys(m).slice(0, 5)))"
```
Expected: une liste de 5 exports (par exemple `['useTranslations', 'useLocale', 'NextIntlClientProvider', 'useFormatter', 'useNow']`). Si erreur d'import → noter le problème, envisager fallback `next-international` (mentionné dans la spec).

- [ ] **Step 4 : Commit**

```bash
git add apps/web/package.json ../../pnpm-lock.yaml
git commit -m "chore(web): add next-intl@^4 dependency"
```

---

## Task 2 : i18n routing config

**Files:**
- Create: `apps/web/src/i18n/routing.ts`
- Create: `apps/web/src/i18n/routing.test.ts`

- [ ] **Step 1 : Écrire le test (qui va échouer car pas encore de fichier)**

Créer [apps/web/src/i18n/routing.test.ts](apps/web/src/i18n/routing.test.ts) :

```typescript
import { describe, it, expect } from 'vitest';
import { routing, locales, defaultLocale } from './routing';

describe('i18n routing', () => {
  it('expose les 4 locales attendues', () => {
    expect(locales).toEqual(['fr', 'en', 'es', 'de']);
  });

  it('définit fr comme locale par défaut', () => {
    expect(defaultLocale).toBe('fr');
  });

  it('configure routing avec localePrefix always', () => {
    expect(routing.locales).toEqual(['fr', 'en', 'es', 'de']);
    expect(routing.defaultLocale).toBe('fr');
    expect(routing.localePrefix).toBe('always');
  });
});
```

- [ ] **Step 2 : Lancer le test pour vérifier qu'il échoue**

```bash
pnpm --filter @nemo/web test src/i18n/routing.test.ts
```
Expected: FAIL avec `Cannot find module './routing'`

- [ ] **Step 3 : Créer la config routing**

Créer [apps/web/src/i18n/routing.ts](apps/web/src/i18n/routing.ts) :

```typescript
import { defineRouting } from 'next-intl/routing';

export const locales = ['fr', 'en', 'es', 'de'] as const;
export type Locale = (typeof locales)[number];
export const defaultLocale: Locale = 'fr';

export const routing = defineRouting({
  locales: [...locales],
  defaultLocale,
  localePrefix: 'always',
});
```

- [ ] **Step 4 : Relancer le test**

```bash
pnpm --filter @nemo/web test src/i18n/routing.test.ts
```
Expected: PASS (3 tests)

- [ ] **Step 5 : Commit**

```bash
git add apps/web/src/i18n/routing.ts apps/web/src/i18n/routing.test.ts
git commit -m "feat(web/i18n): add routing config with 4 locales (fr default, always prefix)"
```

---

## Task 3 : Initial messages files

**Files:**
- Create: `apps/web/messages/fr.json`
- Create: `apps/web/messages/en.json`
- Create: `apps/web/messages/es.json`
- Create: `apps/web/messages/de.json`

- [ ] **Step 1 : Créer fr.json (locale source)**

Créer [apps/web/messages/fr.json](apps/web/messages/fr.json) :

```json
{
  "common": {
    "actions": {
      "save": "Enregistrer",
      "cancel": "Annuler"
    }
  }
}
```

- [ ] **Step 2 : Créer les 3 stubs identiques**

Créer [apps/web/messages/en.json](apps/web/messages/en.json), [apps/web/messages/es.json](apps/web/messages/es.json), [apps/web/messages/de.json](apps/web/messages/de.json), tous avec **exactement le même contenu** que `fr.json` (copie verbatim, traductions différées) :

```json
{
  "common": {
    "actions": {
      "save": "Enregistrer",
      "cancel": "Annuler"
    }
  }
}
```

- [ ] **Step 3 : Vérifier que les 4 fichiers existent et sont identiques**

```bash
diff apps/web/messages/fr.json apps/web/messages/en.json
diff apps/web/messages/fr.json apps/web/messages/es.json
diff apps/web/messages/fr.json apps/web/messages/de.json
```
Expected: aucun output (fichiers identiques)

- [ ] **Step 4 : Commit**

```bash
git add apps/web/messages/
git commit -m "feat(web/i18n): initial messages files for fr/en/es/de (en/es/de = stubs identiques au fr)"
```

---

## Task 4 : i18n request config

**Files:**
- Create: `apps/web/src/i18n/request.ts`

- [ ] **Step 1 : Créer le request config**

Créer [apps/web/src/i18n/request.ts](apps/web/src/i18n/request.ts) :

```typescript
import { getRequestConfig } from 'next-intl/server';
import { routing, defaultLocale, type Locale } from './routing';

export default getRequestConfig(async ({ requestLocale }) => {
  const requested = await requestLocale;
  const locale: Locale = routing.locales.includes(requested as Locale)
    ? (requested as Locale)
    : defaultLocale;

  const messages = (await import(`../../messages/${locale}.json`)).default;

  return { locale, messages };
});
```

- [ ] **Step 2 : Vérifier que le typecheck passe**

```bash
pnpm --filter @nemo/web typecheck
```
Expected: aucune erreur (peut prendre 30s en cold)

- [ ] **Step 3 : Commit**

```bash
git add apps/web/src/i18n/request.ts
git commit -m "feat(web/i18n): add server-side request config for message loading"
```

---

## Task 5 : Wrap next.config avec createNextIntlPlugin

**Files:**
- Modify: `apps/web/next.config.ts`

- [ ] **Step 1 : Modifier next.config.ts**

Remplacer le contenu de [apps/web/next.config.ts](apps/web/next.config.ts) (33 lignes) par :

```typescript
import type { NextConfig } from 'next';
import { resolve } from 'node:path';
import createNextIntlPlugin from 'next-intl/plugin';

const withNextIntl = createNextIntlPlugin('./src/i18n/request.ts');

const config: NextConfig = {
  reactStrictMode: true,
  reactCompiler: true,
  compress: true,
  transpilePackages: ['@nemo/shared-types', '@nemo/game-balance', '@nemo/game-engine-core', '@nemo/routing'],
  typedRoutes: true,
  // Pin the workspace root so Turbopack doesn't walk up and pick a sibling
  // worktree's pnpm-workspace.yaml — keeps dev builds correct when running
  // from .worktrees/*. cwd is apps/web when invoked via pnpm --filter.
  turbopack: {
    root: resolve(process.cwd(), '..', '..'),
  },
  async headers() {
    return [
      {
        source: '/data/:path*',
        headers: [
          { key: 'Cache-Control', value: 'public, max-age=604800, stale-while-revalidate=86400' },
        ],
      },
    ];
  },
};

export default withNextIntl(config);
```

- [ ] **Step 2 : Vérifier que le build est encore vert (les routes existantes ne doivent pas casser)**

```bash
pnpm --filter @nemo/web build
```
Expected: build successful. Si erreur de typecheck sur `next-intl/plugin`, vérifier que Task 1 (install) est bien complété.

- [ ] **Step 3 : Commit**

```bash
git add apps/web/next.config.ts
git commit -m "feat(web/i18n): wrap next config with createNextIntlPlugin"
```

---

## Task 6 : Composer le middleware next-intl dans proxy.ts

**Files:**
- Modify: `apps/web/proxy.ts`

**Contexte** : `proxy.ts` (Next 16.2 successeur de `middleware.ts`) gère déjà l'auth. On doit composer le middleware `next-intl` **avant** la logique d'auth, mais **uniquement** sur les paths préfixés `/(fr|en|es|de)/...`. Les routes existantes (`/marina`, `/races`, etc.) ne sont pas touchées par cette PR.

- [ ] **Step 1 : Modifier proxy.ts pour composer next-intl**

Remplacer le contenu de [apps/web/proxy.ts](apps/web/proxy.ts) par :

```typescript
import type { NextRequest } from 'next/server';
import { NextResponse } from 'next/server';
import createMiddleware from 'next-intl/middleware';
import { routing } from './src/i18n/routing';

/**
 * Next.js 16.2 auth proxy (replaces legacy middleware.ts).
 * Runs at the edge before any route handler.
 *
 * Composition (PR 1 i18n) :
 *   1. Si le path commence par /fr|/en|/es|/de → next-intl middleware (gère
 *      la résolution de locale, set le contexte). Pas de redirect dans cette PR
 *      car les routes existantes vivent encore à la racine.
 *   2. Auth check sur le path original.
 *
 * PR 2 i18n élargira le matcher i18n à toutes les URLs et activera les redirects
 * vers /fr/... pour les paths sans préfixe.
 *
 * Routes publiques (accessibles en mode spectateur, sans cookie) :
 *   - `/`, `/login`, `/register`, `/reset-password`
 *   - `/races` et `/races/*`              → catalogue des courses
 *   - `/ranking` et `/ranking/*`    → classements saison + courses
 *   - `/profile/<username>`               → fiche publique (mais pas
 *     `/profile`, `/profile/settings`, `/profile/social` qui sont privés)
 *
 * Tout le reste (marina, profile perso, play, etc.) exige un cookie
 * `nemo_access_token`. Phase 2 ajoutera la vérification JWT Cognito.
 */

const intlMiddleware = createMiddleware(routing);

const LOCALE_PREFIX_RE = /^\/(fr|en|es|de)(\/|$)/;

const PUBLIC_PATHS = new Set<string>([
  '/',
  '/login',
  '/register',
  '/reset-password',
  '/races',
  '/ranking',
  '/news',
]);

const PUBLIC_PREFIXES = [
  '/_next/',
  '/api/public/',
  '/assets/',
  '/icons/',
  '/favicon',
  '/manifest.webmanifest',
  '/races/',
  '/ranking/',
  '/news/',
];

const PROFILE_PRIVATE_SEGMENTS = new Set<string>(['settings', 'social']);

function isPublic(pathname: string): boolean {
  if (PUBLIC_PATHS.has(pathname)) return true;
  if (PUBLIC_PREFIXES.some((p) => pathname.startsWith(p))) return true;
  if (pathname.startsWith('/profile/')) {
    const seg = pathname.slice('/profile/'.length).split('/')[0];
    if (seg && !PROFILE_PRIVATE_SEGMENTS.has(seg)) return true;
  }
  return false;
}

export default function proxy(request: NextRequest): NextResponse {
  const { pathname } = request.nextUrl;

  // Étape 1 : passer par next-intl si path préfixé locale
  if (LOCALE_PREFIX_RE.test(pathname)) {
    return intlMiddleware(request);
  }

  // Étape 2 : auth check sur les routes existantes (inchangé)
  if (isPublic(pathname)) return NextResponse.next();

  const token = request.cookies.get('nemo_access_token')?.value;
  if (!token) {
    const loginUrl = new URL('/login', request.url);
    loginUrl.searchParams.set('next', pathname);
    return NextResponse.redirect(loginUrl);
  }

  return NextResponse.next();
}

export const config = {
  matcher: [
    '/((?!_next/static|_next/image|favicon.ico|manifest.webmanifest|icons|assets).*)',
  ],
};
```

- [ ] **Step 2 : Vérifier que le typecheck passe**

```bash
pnpm --filter @nemo/web typecheck
```
Expected: aucune erreur

- [ ] **Step 3 : Commit**

```bash
git add apps/web/proxy.ts
git commit -m "feat(web/i18n): compose next-intl middleware in proxy.ts (locale-prefixed paths only)"
```

---

## Task 7 : Shell `[locale]/` placeholder + smoke test

**Files:**
- Create: `apps/web/src/app/[locale]/layout.tsx`
- Create: `apps/web/src/app/[locale]/page.tsx`

**Contexte** : on crée une page placeholder accessible via `/fr`, `/en`, `/es`, `/de`. Cela valide tout le pipeline : middleware → request config → messages → React Server Components avec next-intl. Le placeholder n'utilise pas de string littérale FR (juste le code locale uppercase) pour ne pas violer la règle ESLint qu'on activera en Task 9.

- [ ] **Step 1 : Créer le layout du segment [locale]**

Créer [apps/web/src/app/[locale]/layout.tsx](apps/web/src/app/[locale]/layout.tsx) :

```typescript
import { NextIntlClientProvider } from 'next-intl';
import { getMessages } from 'next-intl/server';
import { notFound } from 'next/navigation';
import { routing, type Locale } from '@/i18n/routing';

export default async function LocaleLayout({
  children,
  params,
}: {
  children: React.ReactNode;
  params: Promise<{ locale: string }>;
}): Promise<React.ReactElement> {
  const { locale } = await params;
  if (!routing.locales.includes(locale as Locale)) notFound();

  const messages = await getMessages();

  return (
    <NextIntlClientProvider locale={locale} messages={messages}>
      {children}
    </NextIntlClientProvider>
  );
}
```

- [ ] **Step 2 : Créer la page placeholder**

Créer [apps/web/src/app/[locale]/page.tsx](apps/web/src/app/[locale]/page.tsx) :

```typescript
import { setRequestLocale, getTranslations } from 'next-intl/server';
import type { Locale } from '@/i18n/routing';

export default async function LocaleHomePage({
  params,
}: {
  params: Promise<{ locale: Locale }>;
}): Promise<React.ReactElement> {
  const { locale } = await params;
  setRequestLocale(locale);

  const t = await getTranslations('common.actions');

  return (
    <main style={{ padding: 24, fontFamily: 'system-ui' }}>
      <h1>{locale.toUpperCase()}</h1>
      <p>{t('save')}</p>
    </main>
  );
}
```

- [ ] **Step 3 : Lancer le dev server et vérifier toutes les locales**

```bash
pnpm --filter @nemo/web dev &
sleep 8
curl -s http://localhost:3000/fr | grep -E "<h1>FR</h1>" || echo "FAIL fr"
curl -s http://localhost:3000/en | grep -E "<h1>EN</h1>" || echo "FAIL en"
curl -s http://localhost:3000/es | grep -E "<h1>ES</h1>" || echo "FAIL es"
curl -s http://localhost:3000/de | grep -E "<h1>DE</h1>" || echo "FAIL de"
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/xx
kill %1
```
Expected:
- 4 lignes contenant `<h1>FR</h1>`, `<h1>EN</h1>`, `<h1>ES</h1>`, `<h1>DE</h1>` (pas de "FAIL xx")
- Dernière ligne : `404` (locale inconnue → notFound)

- [ ] **Step 4 : Vérifier que les routes existantes fonctionnent toujours**

```bash
pnpm --filter @nemo/web dev &
sleep 8
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/login
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/races
kill %1
```
Expected: `200` pour les deux (les routes existantes ne sont pas affectées)

- [ ] **Step 5 : Commit**

```bash
git add apps/web/src/app/\[locale\]/
git commit -m "feat(web/i18n): placeholder [locale]/ shell with NextIntlClientProvider"
```

---

## Task 8 : Étendre vitest config pour les tests dans scripts/

**Files:**
- Modify: `apps/web/vitest.config.ts`

- [ ] **Step 1 : Ajouter scripts/**/*.test.ts à l'include**

Modifier [apps/web/vitest.config.ts](apps/web/vitest.config.ts), bloc `include` :

```typescript
    include: [
      'src/lib/**/*.test.ts',
      'src/hooks/**/*.test.ts',
      'src/components/**/*.test.ts',
      'src/components/**/*.test.tsx',
      'src/app/**/*.test.ts',
      'src/app/**/*.test.tsx',
      'src/i18n/**/*.test.ts',
      'scripts/**/*.test.ts',
    ],
```

- [ ] **Step 2 : Vérifier que vitest découvre toujours les tests existants + les nouveaux emplacements**

```bash
pnpm --filter @nemo/web test --reporter=verbose 2>&1 | head -20
```
Expected: vitest démarre sans erreur. Les tests existants (`src/lib/...`, etc.) restent présents. La nouveauté `src/i18n/routing.test.ts` (créé Task 2) est aussi découverte.

- [ ] **Step 3 : Commit**

```bash
git add apps/web/vitest.config.ts
git commit -m "chore(web): include i18n and scripts test paths in vitest config"
```

---

## Task 9 : Règle ESLint `react/jsx-no-literals` scopée à [locale]/**

**Files:**
- Modify: `apps/web/eslint.config.mjs`

**Contexte** : la règle ne mord que sur les fichiers sous `apps/web/src/app/[locale]/**`. Les routes existantes restent inchangées. La placeholder page créée en Task 7 utilise `{locale.toUpperCase()}` et `{t('save')}` (pas de string littérale) → la règle est respectée.

**Note glob** : `[locale]` dans un glob minimatch est interprété comme classe de caractères. Il faut échapper les crochets : `\\[locale\\]`.

- [ ] **Step 1 : Modifier eslint.config.mjs**

Remplacer le contenu de [apps/web/eslint.config.mjs](apps/web/eslint.config.mjs) par :

```javascript
import nextCoreWebVitals from 'eslint-config-next/core-web-vitals';
import nextTypescript from 'eslint-config-next/typescript';

const config = [
  {
    ignores: [
      '.next/**',
      'node_modules/**',
      'public/**',
      '*.tsbuildinfo',
      'next-env.d.ts',
    ],
  },
  ...nextCoreWebVitals,
  ...nextTypescript,
  {
    rules: {
      'react/no-unescaped-entities': 'off',
      '@typescript-eslint/no-unused-vars': [
        'error',
        { argsIgnorePattern: '^_', varsIgnorePattern: '^_', caughtErrorsIgnorePattern: '^_' },
      ],
      'react-hooks/set-state-in-effect': 'warn',
      'react-hooks/purity': 'warn',
      'react-hooks/immutability': 'warn',
      'react-hooks/preserve-manual-memoization': 'warn',
      '@typescript-eslint/no-explicit-any': 'warn',
      '@typescript-eslint/no-require-imports': 'warn',
    },
  },
  // i18n PR 1 : interdit les strings littérales sous [locale]/**.
  // Le glob s'élargira aux composants et au reste de l'app dans les PR suivantes.
  {
    files: ['src/app/\\[locale\\]/**/*.{tsx,ts}'],
    rules: {
      'react/jsx-no-literals': ['error', {
        noStrings: true,
        ignoreProps: false,
        noAttributeStrings: true,
        allowedStrings: ['—', '·', '•', '/', ':', '|', '×', '→', '↑', '↓', '+', '-'],
      }],
    },
  },
];

export default config;
```

- [ ] **Step 2 : Vérifier que lint passe**

```bash
pnpm --filter @nemo/web lint
```
Expected: aucune erreur. La placeholder de Task 7 n'a pas de string littérale (`{locale.toUpperCase()}` et `{t('save')}` sont des expressions, pas des strings JSX).

- [ ] **Step 3 : Test négatif — vérifier que la règle mord effectivement**

```bash
# Crée temporairement un fichier qui viole la règle
echo 'export default function X(): React.ReactElement { return <div>String en dur</div>; }' > apps/web/src/app/\[locale\]/__test_lint__.tsx
pnpm --filter @nemo/web lint 2>&1 | grep -E "react/jsx-no-literals|String en dur" | head -3
rm apps/web/src/app/\[locale\]/__test_lint__.tsx
```
Expected: au moins une ligne mentionnant `react/jsx-no-literals`. Confirme que la règle est active sur le bon glob.

- [ ] **Step 4 : Commit**

```bash
git add apps/web/eslint.config.mjs
git commit -m "feat(web/i18n): activate react/jsx-no-literals on src/app/[locale]/**"
```

---

## Task 10 : check-translations script — tests d'abord

**Files:**
- Create: `apps/web/scripts/check-translations.test.ts`
- Create: `apps/web/scripts/__fixtures__/missing/messages/fr.json`
- Create: `apps/web/scripts/__fixtures__/missing/messages/en.json`
- Create: `apps/web/scripts/__fixtures__/missing/src/Foo.tsx`
- Create: `apps/web/scripts/__fixtures__/inconsistent/messages/fr.json`
- Create: `apps/web/scripts/__fixtures__/inconsistent/messages/en.json`
- Create: `apps/web/scripts/__fixtures__/orphan/messages/fr.json`
- Create: `apps/web/scripts/__fixtures__/orphan/messages/en.json`
- Create: `apps/web/scripts/__fixtures__/orphan/src/Foo.tsx`
- Create: `apps/web/scripts/__fixtures__/multi-ns/src/Foo.tsx`
- Create: `apps/web/scripts/__fixtures__/event-codes/src/Foo.tsx`

- [ ] **Step 1 : Créer les fixtures pour cas "missing key"**

Créer [apps/web/scripts/__fixtures__/missing/messages/fr.json](apps/web/scripts/__fixtures__/missing/messages/fr.json) :

```json
{ "marina": { "title": "Marina" } }
```

Créer [apps/web/scripts/__fixtures__/missing/messages/en.json](apps/web/scripts/__fixtures__/missing/messages/en.json) :

```json
{ "marina": { "title": "Marina" } }
```

Créer [apps/web/scripts/__fixtures__/missing/src/Foo.tsx](apps/web/scripts/__fixtures__/missing/src/Foo.tsx) :

```tsx
import { useTranslations } from 'next-intl';

export function Foo(): React.ReactElement {
  const t = useTranslations('marina');
  return <div>{t('title')} {t('newButton')}</div>;
}
```

- [ ] **Step 2 : Créer les fixtures pour cas "inconsistent locales"**

Créer [apps/web/scripts/__fixtures__/inconsistent/messages/fr.json](apps/web/scripts/__fixtures__/inconsistent/messages/fr.json) :

```json
{ "common": { "save": "Enregistrer", "cancel": "Annuler" } }
```

Créer [apps/web/scripts/__fixtures__/inconsistent/messages/en.json](apps/web/scripts/__fixtures__/inconsistent/messages/en.json) :

```json
{ "common": { "save": "Enregistrer" } }
```

(en.json manque `common.cancel` → fail attendu)

- [ ] **Step 3 : Créer les fixtures pour cas "orphan key" (warn-only)**

Créer [apps/web/scripts/__fixtures__/orphan/messages/fr.json](apps/web/scripts/__fixtures__/orphan/messages/fr.json) :

```json
{ "marina": { "title": "Marina", "unused": "Jamais référencé" } }
```

Créer [apps/web/scripts/__fixtures__/orphan/messages/en.json](apps/web/scripts/__fixtures__/orphan/messages/en.json) :

```json
{ "marina": { "title": "Marina", "unused": "Jamais référencé" } }
```

Créer [apps/web/scripts/__fixtures__/orphan/src/Foo.tsx](apps/web/scripts/__fixtures__/orphan/src/Foo.tsx) :

```tsx
import { useTranslations } from 'next-intl';
export function Foo(): React.ReactElement {
  const t = useTranslations('marina');
  return <div>{t('title')}</div>;
}
```

- [ ] **Step 4 : Créer les fixtures pour cas multi-namespace dans un même fichier**

Créer [apps/web/scripts/__fixtures__/multi-ns/src/Foo.tsx](apps/web/scripts/__fixtures__/multi-ns/src/Foo.tsx) :

```tsx
import { useTranslations } from 'next-intl';

export function Foo(): React.ReactElement {
  const t = useTranslations('marina');
  const tCommon = useTranslations('common');
  return <div>{t('title')} - {tCommon('save')}</div>;
}
```

(Le parser doit identifier `marina.title` ET `common.save` distinctement.)

- [ ] **Step 5 : Créer les fixtures pour cas tEvent**

Créer [apps/web/scripts/__fixtures__/event-codes/src/Foo.tsx](apps/web/scripts/__fixtures__/event-codes/src/Foo.tsx) :

```tsx
import { tEvent } from '@/lib/i18n-helpers';

export function Foo(): React.ReactElement {
  return <div>{tEvent('wind-shift')}</div>;
}
```

(Le parser doit identifier `events.wind-shift`.)

- [ ] **Step 6 : Écrire les tests**

Créer [apps/web/scripts/check-translations.test.ts](apps/web/scripts/check-translations.test.ts) :

```typescript
import { describe, it, expect } from 'vitest';
import { join, resolve } from 'node:path';
import { extractUsedKeys, validateTranslations } from './check-translations';

const FIXTURES = resolve(__dirname, '__fixtures__');

describe('extractUsedKeys', () => {
  it('extrait clé namespace.key depuis useTranslations + t()', () => {
    const keys = extractUsedKeys(join(FIXTURES, 'missing/src'));
    expect(keys).toContainEqual({ ns: 'marina', key: 'title' });
    expect(keys).toContainEqual({ ns: 'marina', key: 'newButton' });
  });

  it('gère plusieurs useTranslations dans un même fichier', () => {
    const keys = extractUsedKeys(join(FIXTURES, 'multi-ns/src'));
    expect(keys).toContainEqual({ ns: 'marina', key: 'title' });
    expect(keys).toContainEqual({ ns: 'common', key: 'save' });
    expect(keys).not.toContainEqual({ ns: 'marina', key: 'save' });
    expect(keys).not.toContainEqual({ ns: 'common', key: 'title' });
  });

  it('extrait events.code depuis tEvent()', () => {
    const keys = extractUsedKeys(join(FIXTURES, 'event-codes/src'));
    expect(keys).toContainEqual({ ns: 'events', key: 'wind-shift' });
  });
});

describe('validateTranslations', () => {
  it('échoue si une clé utilisée est absente d\'une locale', () => {
    const result = validateTranslations({
      messagesDir: join(FIXTURES, 'missing/messages'),
      srcDir: join(FIXTURES, 'missing/src'),
      locales: ['fr', 'en'],
    });
    expect(result.ok).toBe(false);
    expect(result.errors.some((e) => e.includes('marina.newButton'))).toBe(true);
  });

  it('échoue si une clé manque dans une locale autre que fr (incohérence)', () => {
    const result = validateTranslations({
      messagesDir: join(FIXTURES, 'inconsistent/messages'),
      srcDir: join(FIXTURES, 'inconsistent/src'),  // src vide ok
      locales: ['fr', 'en'],
    });
    expect(result.ok).toBe(false);
    expect(result.errors.some((e) => e.includes('common.cancel') && e.includes('en'))).toBe(true);
  });

  it('warn (sans fail) sur les clés orphelines', () => {
    const result = validateTranslations({
      messagesDir: join(FIXTURES, 'orphan/messages'),
      srcDir: join(FIXTURES, 'orphan/src'),
      locales: ['fr', 'en'],
    });
    expect(result.ok).toBe(true);
    expect(result.warnings.some((w) => w.includes('marina.unused'))).toBe(true);
  });
});
```

- [ ] **Step 7 : Vérifier que les tests échouent (script pas encore implémenté)**

```bash
pnpm --filter @nemo/web test scripts/check-translations
```
Expected: FAIL avec `Cannot find module './check-translations'` (3 describes échouent)

- [ ] **Step 8 : Commit (tests + fixtures, sans implémentation)**

```bash
git add apps/web/scripts/__fixtures__/ apps/web/scripts/check-translations.test.ts
git commit -m "test(web/i18n): failing tests + fixtures for check-translations script"
```

---

## Task 11 : check-translations script — implémentation

**Files:**
- Create: `apps/web/scripts/check-translations.ts`

- [ ] **Step 1 : Implémenter le script**

Créer [apps/web/scripts/check-translations.ts](apps/web/scripts/check-translations.ts) :

```typescript
import * as ts from 'typescript';
import { readFileSync, readdirSync, statSync, existsSync } from 'node:fs';
import { join, extname, resolve, dirname } from 'node:path';
import { fileURLToPath } from 'node:url';

export interface UsedKey {
  ns: string;
  key: string;
}

export interface ValidationOptions {
  messagesDir: string;
  srcDir: string;
  locales: readonly string[];
}

export interface ValidationResult {
  ok: boolean;
  errors: string[];
  warnings: string[];
}

const TRANSLATION_FN_NAMES = new Set(['useTranslations', 'getTranslations']);

function walkFiles(dir: string, exts: readonly string[]): string[] {
  if (!existsSync(dir)) return [];
  const out: string[] = [];
  const stack = [dir];
  while (stack.length) {
    const current = stack.pop()!;
    for (const entry of readdirSync(current)) {
      if (entry === 'node_modules' || entry.startsWith('.next')) continue;
      const full = join(current, entry);
      const stat = statSync(full);
      if (stat.isDirectory()) stack.push(full);
      else if (exts.includes(extname(full))) out.push(full);
    }
  }
  return out;
}

function extractFromFile(filePath: string): UsedKey[] {
  const source = readFileSync(filePath, 'utf-8');
  const sf = ts.createSourceFile(filePath, source, ts.ScriptTarget.Latest, true);
  const bindings = new Map<string, string>();
  const used: UsedKey[] = [];

  function visit(node: ts.Node): void {
    // const t = useTranslations('namespace')
    if (ts.isVariableDeclaration(node) && node.initializer && ts.isIdentifier(node.name)) {
      const init = node.initializer;
      // also handle: const t = await getTranslations(...)
      const callExpr = ts.isAwaitExpression(init) ? init.expression : init;
      if (ts.isCallExpression(callExpr) && ts.isIdentifier(callExpr.expression)) {
        if (TRANSLATION_FN_NAMES.has(callExpr.expression.text)) {
          const arg = callExpr.arguments[0];
          if (arg && ts.isStringLiteral(arg)) {
            bindings.set(node.name.text, arg.text);
          } else if (arg && ts.isObjectLiteralExpression(arg)) {
            // getTranslations({ locale, namespace: 'marina' })
            for (const prop of arg.properties) {
              if (
                ts.isPropertyAssignment(prop) &&
                ts.isIdentifier(prop.name) &&
                prop.name.text === 'namespace' &&
                ts.isStringLiteral(prop.initializer)
              ) {
                bindings.set(node.name.text, prop.initializer.text);
              }
            }
          }
        }
      }
    }

    // t('key') ou tEvent('code')
    if (ts.isCallExpression(node) && ts.isIdentifier(node.expression)) {
      const fnName = node.expression.text;
      if (bindings.has(fnName)) {
        const arg = node.arguments[0];
        if (arg && ts.isStringLiteral(arg)) {
          used.push({ ns: bindings.get(fnName)!, key: arg.text });
        }
      } else if (fnName === 'tEvent') {
        const arg = node.arguments[0];
        if (arg && ts.isStringLiteral(arg)) {
          used.push({ ns: 'events', key: arg.text });
        }
      }
    }

    ts.forEachChild(node, visit);
  }

  visit(sf);
  return used;
}

export function extractUsedKeys(srcDir: string): UsedKey[] {
  const files = walkFiles(srcDir, ['.ts', '.tsx']);
  const all: UsedKey[] = [];
  for (const f of files) {
    if (f.endsWith('.test.ts') || f.endsWith('.test.tsx')) continue;
    all.push(...extractFromFile(f));
  }
  return all;
}

function flattenKeys(obj: unknown, prefix = ''): string[] {
  if (obj === null || typeof obj !== 'object') return [];
  const out: string[] = [];
  for (const [k, v] of Object.entries(obj as Record<string, unknown>)) {
    const path = prefix ? `${prefix}.${k}` : k;
    if (v !== null && typeof v === 'object' && !Array.isArray(v)) {
      out.push(...flattenKeys(v, path));
    } else {
      out.push(path);
    }
  }
  return out;
}

function loadLocaleKeys(messagesDir: string, locale: string): Set<string> {
  const file = join(messagesDir, `${locale}.json`);
  if (!existsSync(file)) {
    throw new Error(`Missing messages file: ${file}`);
  }
  const data = JSON.parse(readFileSync(file, 'utf-8'));
  return new Set(flattenKeys(data));
}

export function validateTranslations(opts: ValidationOptions): ValidationResult {
  const errors: string[] = [];
  const warnings: string[] = [];

  const used = extractUsedKeys(opts.srcDir);
  const usedSet = new Set(used.map((u) => `${u.ns}.${u.key}`));

  const localeSets = new Map<string, Set<string>>();
  for (const locale of opts.locales) {
    localeSets.set(locale, loadLocaleKeys(opts.messagesDir, locale));
  }

  // 1. Missing : clé utilisée mais absente d'une locale
  for (const fullKey of usedSet) {
    for (const locale of opts.locales) {
      if (!localeSets.get(locale)!.has(fullKey)) {
        errors.push(`Missing key ${fullKey} in messages/${locale}.json`);
      }
    }
  }

  // 2. Inconsistent : clé présente dans fr.json mais absente d'une autre locale
  const frKeys = localeSets.get('fr');
  if (frKeys) {
    for (const key of frKeys) {
      for (const locale of opts.locales) {
        if (locale === 'fr') continue;
        if (!localeSets.get(locale)!.has(key)) {
          errors.push(`Inconsistent: key ${key} present in fr.json but missing in ${locale}.json`);
        }
      }
    }
  }

  // 3. Orphan : clé présente mais jamais utilisée (warn)
  if (frKeys) {
    for (const key of frKeys) {
      if (!usedSet.has(key)) {
        warnings.push(`Orphan key ${key} (in messages but never referenced in code)`);
      }
    }
  }

  return { ok: errors.length === 0, errors, warnings };
}

// CLI entry
function main(): void {
  const here = dirname(fileURLToPath(import.meta.url));
  const repoRoot = resolve(here, '..');
  const result = validateTranslations({
    messagesDir: join(repoRoot, 'messages'),
    srcDir: join(repoRoot, 'src'),
    locales: ['fr', 'en', 'es', 'de'],
  });

  for (const w of result.warnings) console.warn(`⚠ ${w}`);
  for (const e of result.errors) console.error(`✗ ${e}`);

  if (result.ok) {
    const usedCount = extractUsedKeys(join(repoRoot, 'src')).length;
    console.log(`✓ ${usedCount} clés utilisées, ${result.warnings.length} orphelines, 4 locales complètes`);
    process.exit(0);
  } else {
    console.error(`\n✗ ${result.errors.length} erreur(s) i18n`);
    process.exit(1);
  }
}

// ESM-safe "is main module" check (project compile en module: ESNext)
if (process.argv[1] === fileURLToPath(import.meta.url)) main();
```

- [ ] **Step 2 : Lancer les tests, vérifier qu'ils passent**

```bash
pnpm --filter @nemo/web test scripts/check-translations
```
Expected: PASS (6 tests : 3 describe extractUsedKeys + 3 describe validateTranslations)

- [ ] **Step 3 : Installer `tsx` dans apps/web pour pouvoir exécuter le script en CLI**

```bash
pnpm --filter @nemo/web add -D tsx
```

- [ ] **Step 4 : Run le script en CLI sur le repo réel pour vérifier qu'il ne plante pas**

```bash
cd apps/web && pnpm exec tsx scripts/check-translations.ts
```
Expected: `✓ 1 clés utilisées, 0 orphelines, 4 locales complètes` (la seule clé utilisée à ce stade est `common.actions.save` dans la placeholder de Task 7).

- [ ] **Step 5 : Commit**

```bash
git add apps/web/scripts/check-translations.ts apps/web/package.json
git add ../../pnpm-lock.yaml 2>/dev/null || true
git commit -m "feat(web/i18n): implement check-translations script (TS Compiler API based)"
```

---

## Task 12 : Wire up `pnpm i18n:check`, CI step, et CLAUDE.md

**Files:**
- Modify: `apps/web/package.json`
- Modify: `apps/web/tsconfig.json`
- Modify: `.github/workflows/ci.yml`
- Modify: `CLAUDE.md`

- [ ] **Step 1a : Inclure `scripts/**/*.ts` dans le typecheck de apps/web**

`tsx` a été ajouté en Task 11 Step 3. On étend maintenant l'`include` de tsconfig pour que `pnpm typecheck` couvre le script.

Modifier [apps/web/tsconfig.json](apps/web/tsconfig.json), bloc `include` :

```json
  "include": [
    "next-env.d.ts",
    ".next/types/**/*.ts",
    "next.config.ts",
    "proxy.ts",
    "src/**/*.ts",
    "src/**/*.tsx",
    "scripts/**/*.ts"
  ],
```

Vérifier que `pnpm --filter @nemo/web typecheck` passe avec le nouveau include (le script `check-translations.ts` doit typer proprement).

```bash
pnpm --filter @nemo/web typecheck
```
Expected: aucune erreur

- [ ] **Step 1c : Ajouter le script `i18n:check` à package.json**

Modifier [apps/web/package.json](apps/web/package.json), section `scripts` :

```json
  "scripts": {
    "sync-game-balance": "node ../../scripts/sync-game-balance.mjs",
    "predev": "pnpm sync-game-balance",
    "prebuild": "pnpm sync-game-balance",
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "i18n:check": "tsx scripts/check-translations.ts"
  },
```

- [ ] **Step 2 : Vérifier que `pnpm i18n:check` tourne localement**

```bash
pnpm --filter @nemo/web i18n:check
```
Expected: `✓ 1 clés utilisées, 0 orphelines, 4 locales complètes` + exit 0

- [ ] **Step 3 : Ajouter le step CI**

Modifier [.github/workflows/ci.yml](.github/workflows/ci.yml), ajouter un step après `Test` :

```yaml
      - name: Test
        run: pnpm test

      - name: i18n check
        run: pnpm --filter @nemo/web i18n:check
```

- [ ] **Step 4 : Mettre à jour CLAUDE.md**

Ajouter à la fin de [CLAUDE.md](CLAUDE.md) (après la section graphify) :

```markdown

## i18n

Ce projet utilise `next-intl` v4 pour l'internationalisation. 4 locales : `fr` (source), `en`, `es`, `de` (stubs jusqu'à traduction).

**Règle absolue** : aucune string littérale en dur dans le code UI. Tout texte affiché à l'utilisateur passe par une clé de traduction.

- Côté UI : `const t = useTranslations('namespace')` + `t('key')`. Server components : `await getTranslations('namespace')`.
- Côté événements backend : `tEvent('event-code')` qui résout `events.{code}` dans les messages.
- Fichiers de messages : [apps/web/messages/{fr,en,es,de}.json](apps/web/messages/), structure namespacée 1:1 avec les routes.
- Garde-fou local : `pnpm --filter @nemo/web lint` (règle ESLint `react/jsx-no-literals` active sur `src/app/[locale]/**`).
- Garde-fou CI : `pnpm --filter @nemo/web i18n:check` vérifie que toutes les clés référencées existent dans les 4 locales et flag les clés orphelines.
- Quand tu ajoutes/édites un wording : crée la clé dans `messages/fr.json` ET copie-la dans `en.json`, `es.json`, `de.json` (mêmes valeurs, traduction différée).

Spec design : [docs/superpowers/specs/2026-04-28-i18n-design.md](docs/superpowers/specs/2026-04-28-i18n-design.md).
```

- [ ] **Step 5 : Commit**

```bash
git add apps/web/package.json apps/web/tsconfig.json .github/workflows/ci.yml CLAUDE.md
git commit -m "ci(web/i18n): wire pnpm i18n:check + CI step + tsconfig include + CLAUDE.md section"
```

---

## Task 13 : Validation d'intégration finale

**Files:** aucun (validation)

- [ ] **Step 1 : Typecheck complet du repo**

```bash
pnpm typecheck
```
Expected: aucune erreur

- [ ] **Step 2 : Lint complet du repo**

```bash
pnpm lint
```
Expected: aucune erreur

- [ ] **Step 3 : Tests complets du repo**

```bash
pnpm test
```
Expected: tous les tests passent (les 9 nouveaux i18n + tous les existants)

- [ ] **Step 4 : Build complet**

```bash
pnpm --filter @nemo/web build
```
Expected: build successful

- [ ] **Step 5 : Smoke manuel sur les 4 locales + routes existantes**

```bash
pnpm --filter @nemo/web dev &
sleep 8
echo "--- /fr ---"; curl -s http://localhost:3000/fr | grep -E "<h1>|save"
echo "--- /en ---"; curl -s http://localhost:3000/en | grep -E "<h1>|save"
echo "--- /login (route existante) ---"; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/login
echo "--- /races (route existante) ---"; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/races
echo "--- /xx (locale inconnue) ---"; curl -s -o /dev/null -w "%{http_code}\n" http://localhost:3000/xx
kill %1
```
Expected:
- `/fr` et `/en` → contenu placeholder (`<h1>FR</h1>` ou `<h1>EN</h1>`, le mot "Enregistrer")
- `/login` → 200
- `/races` → 200
- `/xx` → 404

- [ ] **Step 6 : i18n check final**

```bash
pnpm --filter @nemo/web i18n:check
```
Expected: `✓ 1 clés utilisées, 0 orphelines, 4 locales complètes`

- [ ] **Step 7 : Pas de commit si tout est vert**

Si tous les checks passent, aucun commit nécessaire (le travail des Tasks 1-12 est déjà commit). Si un check révèle un problème, corriger inline et commit "fix(...)" approprié.

---

## Definition of Done — PR 1

- [x] `next-intl@^4` installé, vérifié compat Next 16.2 + React 19
- [x] `apps/web/src/i18n/{routing,request}.ts` créés et testés
- [x] `apps/web/messages/{fr,en,es,de}.json` créés (4 fichiers identiques avec `common.actions.save` + `cancel`)
- [x] `apps/web/proxy.ts` compose le middleware next-intl pour les paths `/(fr|en|es|de)/...`
- [x] `apps/web/src/app/[locale]/{layout,page}.tsx` placeholder fonctionnel
- [x] Routes existantes (`/login`, `/races`, etc.) inchangées (200 OK)
- [x] Règle ESLint `react/jsx-no-literals` active sur `src/app/[locale]/**` uniquement
- [x] `pnpm i18n:check` passe localement et en CI
- [x] CLAUDE.md mis à jour avec la règle "plus jamais de string en dur"
- [x] `pnpm typecheck`, `pnpm lint`, `pnpm test`, `pnpm build` tous verts

---

## Plans suivants (à écrire après merge de PR 1)

| Plan | Scope |
|---|---|
| Plan 2 | Shell + déplacement physique des routes vers `[locale]/` + élargissement matcher middleware + sélecteur de langue + colonne `user_settings.locale` |
| Plan 3 | Migration des composants `components/ui/` (boutons, modals, toasts, footer, topbar) |
| Plan 4 | Migration des pages publiques + légales (home, login, news, cgu, cookies, legal, privacy) |
| Plan 5 | Migration des pages joueur authentifié (marina, races, ranking, profile, team) |
| Plan 6 | Migration de `/play/[raceId]` |
| Plan 7 | Refacto `game-balance.json` + extraction `boat-catalog.{locale}.json` + audit neutralité backend + helper `tEvent()` |
